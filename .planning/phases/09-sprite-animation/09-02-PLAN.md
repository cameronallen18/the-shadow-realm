---
phase: 9
plan: "09-02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - components/samus-run/canvas/drawSamus.ts
  - components/samus-run/constants.ts
autonomous: false
requirements:
  - QUAL-02

must_haves:
  truths:
    - "COLLISION.samusWidth and COLLISION.samusHeight match the visible sprite body (excluding transparent padding and arm cannon)"
    - "DEBUG_HITBOX flag is false in the committed file"
    - "Collision hitbox rect visually hugs the real sprite body when DEBUG_HITBOX is temporarily enabled"
  artifacts:
    - path: "components/samus-run/constants.ts"
      provides: "Updated COLLISION.samusWidth and COLLISION.samusHeight tuned to real sprite"
      contains: "samusWidth:"
    - path: "components/samus-run/canvas/drawSamus.ts"
      provides: "DEBUG_HITBOX = false in committed state"
      contains: "DEBUG_HITBOX = false"
  key_links:
    - from: "DEBUG_HITBOX flag (drawSamus.ts)"
      to: "COLLISION.samusWidth / samusHeight (constants.ts)"
      via: "visual tuning workflow — flip flag, observe, update constants, flip back"
      pattern: "DEBUG_HITBOX = false"
---

<objective>
Tune COLLISION.samusWidth and COLLISION.samusHeight to match the real sprite body dimensions using the DEBUG_HITBOX visual overlay workflow. Delivers QUAL-02: hitbox constants updated to match the visible Samus sprite body (excluding transparent padding and arm cannon).

Purpose: The current COLLISION constants (samusWidth=28, samusHeight=36) were sized for the procedural shape-Samus from Phase 7. The real sprite's content area is 81×81px but the actual Samus body occupies a sub-region of that. Hitbox accuracy directly affects gameplay fairness — a hitbox that is too large creates unfair deaths; too small creates false survival.

Output:
- `canvas/drawSamus.ts` — DEBUG_HITBOX = false confirmed in committed state
- `constants.ts` — COLLISION.samusWidth and samusHeight updated to values confirmed by visual overlay
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-sprite-animation/09-CONTEXT.md
@.planning/phases/09-sprite-animation/09-UI-SPEC.md
@.planning/phases/09-sprite-animation/09-01-SUMMARY.md

<interfaces>
<!-- Key constants the executor needs. Extracted from codebase. -->

From components/samus-run/constants.ts (current values — placeholders to be tuned):
```typescript
export const COLLISION = {
  hitboxScale: 0.65,   // unchanged — 65% reduction stays
  samusWidth: 28,      // PLACEHOLDER — set for shape-Samus; must be tuned to real sprite
  samusHeight: 36,     // PLACEHOLDER — set for shape-Samus; must be tuned to real sprite
} as const;
```

From components/samus-run/canvas/drawSamus.ts (DEBUG_HITBOX location):
```typescript
const DEBUG_HITBOX = false;  // line near top of drawSamusSprite section

// DEBUG_HITBOX block inside drawSamusSprite:
if (DEBUG_HITBOX) {
  ctx.save();
  ctx.strokeStyle = "rgba(255, 0, 0, 0.8)";
  ctx.lineWidth = 1;
  ctx.strokeRect(
    Math.floor(x - COLLISION.samusWidth / 2),
    Math.floor(y - COLLISION.samusHeight),
    COLLISION.samusWidth,
    COLLISION.samusHeight
  );
  ctx.restore();
}
```

Expected tuned range (from RESEARCH.md): samusWidth ~30-45px, samusHeight ~40-55px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DEBUG_HITBOX infrastructure is in place</name>
  <files>components/samus-run/canvas/drawSamus.ts</files>

  <read_first>
    - components/samus-run/canvas/drawSamus.ts (verify drawSamusSprite was added by Plan 09-01 and DEBUG_HITBOX block is present)
  </read_first>

  <action>
Read `drawSamus.ts` and confirm the following are present from Plan 09-01:

1. `const DEBUG_HITBOX = false;` — the constant exists and is `false`
2. Inside `drawSamusSprite`, an `if (DEBUG_HITBOX)` block that calls `ctx.strokeRect` using `COLLISION.samusWidth` and `COLLISION.samusHeight`
3. The strokeRect arguments use the correct coordinate formula:
   - x: `Math.floor(x - COLLISION.samusWidth / 2)` — centered on Samus X
   - y: `Math.floor(y - COLLISION.samusHeight)` — anchored at samusY (bottom of sprite, matching sprite anchor convention)
   - width: `COLLISION.samusWidth`
   - height: `COLLISION.samusHeight`
4. `ctx.strokeStyle = "rgba(255, 0, 0, 0.8)"` — red overlay as specified in D-07

If any of these are missing or incorrect, fix them now before the human checkpoint proceeds. No new logic — only ensure the infrastructure added in 09-01 is correct.

If everything is present and correct, this task is complete with no file changes.
  </action>

  <verify>
    <automated>grep -n "DEBUG_HITBOX\|strokeRect\|samusWidth\|samusHeight\|rgba(255, 0, 0" /home/cameron/projects/the-shadow-realm/components/samus-run/canvas/drawSamus.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "DEBUG_HITBOX = false" components/samus-run/canvas/drawSamus.ts` returns exactly 1 match
    - `grep "if (DEBUG_HITBOX)" components/samus-run/canvas/drawSamus.ts` returns exactly 1 match
    - `grep "strokeRect" components/samus-run/canvas/drawSamus.ts` returns a match inside the DEBUG_HITBOX block
    - `grep "rgba(255, 0, 0" components/samus-run/canvas/drawSamus.ts` returns a match (red overlay)
    - `grep "COLLISION.samusWidth / 2\|COLLISION.samusHeight" components/samus-run/canvas/drawSamus.ts` returns matches confirming the hitbox uses COLLISION constants, not hardcoded values
  </acceptance_criteria>

  <done>DEBUG_HITBOX infrastructure confirmed present and correct: flag is false, strokeRect block uses COLLISION constants with correct coordinate formulas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual hitbox tuning — flip DEBUG_HITBOX, measure, update constants</name>

  <what-built>
Plan 09-01 added `drawSamusSprite` with a `DEBUG_HITBOX` overlay that renders a red rectangle over Samus using the current COLLISION.samusWidth and COLLISION.samusHeight constants. Task 1 verified the infrastructure is correct. The current placeholder values (samusWidth=28, samusHeight=36) were sized for the shape-Samus and will not match the real sprite.
  </what-built>

  <how-to-verify>
**Step 1 — Enable the debug overlay:**

In `components/samus-run/canvas/drawSamus.ts`, change line:
```typescript
const DEBUG_HITBOX = false;
```
to:
```typescript
const DEBUG_HITBOX = true;
```

**Step 2 — Start the dev server:**
```
npm run dev
```
Open `http://localhost:3000/projects/samus-run` in your browser.

**Step 3 — Observe the hitbox:**

A red rectangle should appear overlaid on Samus's sprite position. Compare the red rect to the visible Samus body on screen.

**Step 4 — Tune the values:**

Open `components/samus-run/constants.ts`. Update `COLLISION.samusWidth` and `COLLISION.samusHeight` until the red rectangle closely hugs the visible sprite body (torso + legs, excluding the arm cannon extension on the right side and transparent padding around the sprite).

Expected range: `samusWidth` around 30-45px, `samusHeight` around 40-55px.

Save the file with updated values — the browser hot-reloads automatically so you can see the rect update in real time.

**Step 5 — Verify the tuned hitbox:**

The red rectangle should:
- Cover the main body (torso, helmet, legs)
- NOT extend far into the transparent margin on the left/top/bottom
- NOT include the full arm cannon extension (some cannon overlap is acceptable for fairness)
- Be centered on Samus's visual center

**Step 6 — Bake and clean up:**

Once satisfied with the hitbox alignment, set `DEBUG_HITBOX = false` in `drawSamus.ts` and save. The red rectangle should disappear. Confirm the game plays correctly with no visible debug overlay.
  </how-to-verify>

  <resume-signal>
Type "approved" when the hitbox is tuned and DEBUG_HITBOX is set back to false, and provide the final values you used:
  - samusWidth: [value]
  - samusHeight: [value]

Or describe any issues encountered.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit final constants and confirm clean state</name>
  <files>components/samus-run/constants.ts, components/samus-run/canvas/drawSamus.ts</files>

  <read_first>
    - components/samus-run/constants.ts (confirm the user's tuned samusWidth and samusHeight values are saved)
    - components/samus-run/canvas/drawSamus.ts (confirm DEBUG_HITBOX = false — must not be true in the committed file)
  </read_first>

  <action>
Verify that the user's changes from Task 2 are correctly saved and the state is clean:

1. Read `constants.ts` — confirm `COLLISION.samusWidth` and `COLLISION.samusHeight` have the tuned values (NOT the placeholder values 28 and 36, unless the user explicitly confirmed those are correct after visual tuning).

2. Read `drawSamus.ts` — confirm `DEBUG_HITBOX = false` (not true). If it is still `true` from the tuning session, change it back to `false` now.

3. Run TypeScript check: `npx tsc --noEmit` — must exit 0.

4. Run `npm run build` — must succeed with no errors.

No other code changes. This task is purely verification + cleanup of the debug flag.
  </action>

  <verify>
    <automated>cd /home/cameron/projects/the-shadow-realm && grep "DEBUG_HITBOX = false" components/samus-run/canvas/drawSamus.ts && grep "samusWidth\|samusHeight" components/samus-run/constants.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep "DEBUG_HITBOX = false" components/samus-run/canvas/drawSamus.ts` returns exactly 1 match — flag is off
    - `grep "DEBUG_HITBOX = true" components/samus-run/canvas/drawSamus.ts` returns NO matches
    - `grep "samusWidth:" components/samus-run/constants.ts` returns a value that is NOT 28 (unless user confirmed 28 after visual inspection — note the confirmed value)
    - `grep "samusHeight:" components/samus-run/constants.ts` returns a value that is NOT 36 (unless user confirmed 36 after visual inspection — note the confirmed value)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>COLLISION.samusWidth and samusHeight contain the user-confirmed tuned values. DEBUG_HITBOX = false in committed state. TypeScript and build pass. QUAL-02 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | This plan modifies only constant values and a compile-time debug flag. No network, no auth, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | `DEBUG_HITBOX = true` accidentally committed | mitigate | Task 3 acceptance criteria explicitly verify `grep "DEBUG_HITBOX = false"` passes and `grep "DEBUG_HITBOX = true"` returns no match before completion. |
| T-09-06 | Denial of Service | N/A | accept | Constant value changes only; zero performance impact. |

**ASVS L1 note:** This plan introduces zero auth, zero user data, zero server interaction. The only security-relevant item is the DEBUG_HITBOX compile-time gate (T-09-05), which is enforced by acceptance criteria.
</threat_model>

<verification>
After all tasks complete:

1. `grep "DEBUG_HITBOX = false" components/samus-run/canvas/drawSamus.ts` — returns 1 match
2. `grep "DEBUG_HITBOX = true" components/samus-run/canvas/drawSamus.ts` — returns 0 matches
3. `grep "samusWidth:\|samusHeight:" components/samus-run/constants.ts` — returns tuned values
4. `npx tsc --noEmit` exits 0
5. `npm run build` exits 0
6. Manual: run dev server, confirm no red hitbox rect visible during gameplay
</verification>

<success_criteria>
- QUAL-02: COLLISION.samusWidth and COLLISION.samusHeight updated to match the real sprite body — confirmed by user visual inspection with DEBUG_HITBOX overlay
- DEBUG_HITBOX = false in committed state — confirmed by grep
- TypeScript and build clean — confirmed by npx tsc --noEmit and npm run build
</success_criteria>

<output>
After completion, create `.planning/phases/09-sprite-animation/09-02-SUMMARY.md`
</output>
