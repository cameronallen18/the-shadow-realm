---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/favicon.ico
  - public/
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - .gitignore
  - README.md
  - next-env.d.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
user_setup:
  - service: vercel
    why: "Hosting + GitHub auto-deploy CI/CD"
    env_vars: []
    dashboard_config:
      - task: "Import GitHub repo cameronallen18/the-shadow-realm at https://vercel.com/new"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository"
      - task: "Confirm framework auto-detect = Next.js, accept default Build Command (next build), leave Output Directory blank, click Deploy"
        location: "Vercel Dashboard -> Configure Project"

must_haves:
  truths:
    - "Next.js 15 App Router project files exist at repo root with TypeScript and Tailwind v4"
    - "All scaffold output is committed to git history alongside pre-existing .planning/, CLAUDE.md, README.md preservation"
    - "Repo is pushed to cameronallen18/the-shadow-realm main branch on GitHub"
    - "Vercel project is linked to GitHub repo and the latest push to main produces a successful production deploy"
    - "A live Vercel URL returns HTTP 200 and renders the default Next.js page in a browser"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root App Router layout with html/body shell and Geist font"
      contains: "RootLayout"
    - path: "app/page.tsx"
      provides: "Default landing page rendered at /"
      contains: "export default"
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS-first import"
      contains: "@import \"tailwindcss\""
    - path: "next.config.ts"
      provides: "Empty Next.js config (no output:export, no custom image config)"
      contains: "NextConfig"
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin wiring"
      contains: "@tailwindcss/postcss"
    - path: "package.json"
      provides: "Pinned Next.js 15.x, React 19.x, Tailwind v4 dependencies + scripts"
      contains: "\"next\""
    - path: ".git/"
      provides: "Preserved git history with original commits intact"
  key_links:
    - from: "GitHub repo (cameronallen18/the-shadow-realm main)"
      to: "Vercel project"
      via: "Vercel GitHub App webhook installed via dashboard import"
      pattern: "push to main triggers Production deploy"
    - from: "Vercel deploy"
      to: "live public URL"
      via: "Vercel native Next.js adapter"
      pattern: "https://*.vercel.app returns 200 OK"
---

<objective>
Scaffold a Next.js 15 App Router project (TypeScript + Tailwind v4) into the existing repo, push to GitHub, and connect the repo to Vercel so every push to `main` produces a live production deploy.

Purpose: Prove the deploy chain end-to-end before any UI work begins. Phase 2 (landing page) and Phase 3 (catalog) build on top of a confirmed CI/CD pipeline.

Output:
- Working Next.js 15 project at repo root with all default scaffold files
- Code committed to local git with pre-existing files preserved (.planning/, CLAUDE.md)
- Code pushed to cameronallen18/the-shadow-realm on GitHub
- Vercel project linked to the repo, first deploy successful
- Public Vercel URL verified loading in browser
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 with the .git relocation workaround</name>
  <files>
    Created by create-next-app (do not hand-write):
    - app/layout.tsx
    - app/page.tsx
    - app/globals.css
    - app/favicon.ico
    - public/ (SVG assets)
    - package.json
    - package-lock.json
    - tsconfig.json
    - next.config.ts
    - postcss.config.mjs
    - eslint.config.mjs
    - .gitignore
    - README.md (will overwrite the existing empty one — expected)
    - next-env.d.ts
  </files>
  <read_first>
    - /home/cameron/projects/the-shadow-realm/.planning/phases/01-foundation/01-RESEARCH.md (especially "Architecture Patterns: Scaffolding Into an Existing Git Repository" and "Common Pitfalls 1, 2, 4, 5")
    - /home/cameron/projects/the-shadow-realm/CLAUDE.md (locked stack decisions)
    - Run `ls -la /home/cameron/projects/the-shadow-realm/` to confirm pre-existing files (.git, .planning, CLAUDE.md, README.md)
    - Run `node --version && npm --version` to confirm Node >= 18.18 (research verified v24.14.1 and 11.11.0)
  </read_first>
  <action>
Execute the EXACT scaffold sequence from RESEARCH.md, in this order:

1. Verify state, then move .git out of the way:
```bash
ls -la /home/cameron/projects/the-shadow-realm
mv /home/cameron/projects/the-shadow-realm/.git /tmp/shadow-realm-git-backup
```

2. Run create-next-app pinned to Next.js 15 (DO NOT use @latest — that resolves to 16.2.3). Use the exact flag set verified in research. Note: research specified `--turbopack` but for Vercel build stability on first deploy, omit it (Vercel uses webpack for production builds regardless; turbopack is dev-only). Use `--no-turbopack` explicitly if the prompt appears, otherwise the flag-only invocation below skips it:

```bash
cd /home/cameron/projects/the-shadow-realm
npx --yes create-next-app@next-15-3 . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --no-src-dir \
  --import-alias "@/*" \
  --no-git
```

The `--no-git` flag prevents create-next-app from trying to initialize a fresh git repo (we want our existing one). If create-next-app prompts about overwriting README.md or any existing file, accept the overwrite (the existing README.md is empty/placeholder per research).

3. Restore the original git history:
```bash
mv /tmp/shadow-realm-git-backup /home/cameron/projects/the-shadow-realm/.git
```

4. Verify git history is intact and the working tree shows the scaffold output as new/modified files:
```bash
cd /home/cameron/projects/the-shadow-realm
git log --oneline -5
git status
```

5. Verify the scaffold produced the expected versions and structure:
```bash
cd /home/cameron/projects/the-shadow-realm
cat package.json | grep -E '"next"|"react"|"tailwindcss"|"@tailwindcss/postcss"|"typescript"'
cat next.config.ts
cat postcss.config.mjs
cat app/globals.css
ls app/
```

Expected results:
- `package.json` has `"next": "15.x.x"` (NOT 16.x.x), `"react": "19.x.x"`, `"tailwindcss": "^4.x.x"`, `"@tailwindcss/postcss": "^4.x.x"`, `"typescript": "^5.x.x"`
- `next.config.ts` contains `const nextConfig: NextConfig = {};` with NO `output: 'export'` and NO image config
- `postcss.config.mjs` references `@tailwindcss/postcss`
- `app/globals.css` contains `@import "tailwindcss"` (Tailwind v4 CSS-first format)
- `app/` contains `layout.tsx`, `page.tsx`, `globals.css`, `favicon.ico`
- `git log --oneline -5` shows the pre-existing commits (not a fresh repo)
- `git status` shows untracked Next.js files plus modified README.md

6. Do NOT modify ANY scaffold output before the first deploy (Pitfall 5 — committing the unmodified scaffold guarantees the build passes, which proves the pipeline before any custom code is layered on).

7. Run a local build to confirm the scaffold compiles cleanly before pushing:
```bash
cd /home/cameron/projects/the-shadow-realm
npm run build
```

If `npm run build` fails, STOP and report the error — do NOT push a broken build.
  </action>
  <verify>
    <automated>cd /home/cameron/projects/the-shadow-realm && test -f app/layout.tsx && test -f app/page.tsx && test -f next.config.ts && test -f postcss.config.mjs && grep -q '@import "tailwindcss"' app/globals.css && grep -q '"next": "15' package.json && ! grep -q "output.*export" next.config.ts && test -d .git && git log --oneline -1 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `app/favicon.ico` all exist (verified: `test -f`)
    - `package.json` contains `"next": "15.` (NOT `"next": "16.`) — verify with `grep '"next": "15' package.json`
    - `package.json` contains `"react": "19.` — verify with `grep '"react": "19' package.json`
    - `package.json` contains `"@tailwindcss/postcss"` — verify with `grep '@tailwindcss/postcss' package.json`
    - `next.config.ts` does NOT contain the string `output` (confirms no `output: 'export'`) — verify with `! grep "output" next.config.ts`
    - `app/globals.css` contains the literal line `@import "tailwindcss";` — verify with `grep '@import "tailwindcss"' app/globals.css`
    - `postcss.config.mjs` contains `@tailwindcss/postcss` — verify with `grep '@tailwindcss/postcss' postcss.config.mjs`
    - `.git` directory exists at repo root and `git log --oneline -1` succeeds (history preserved) — verify with `test -d .git && git log --oneline -1`
    - `npm run build` exits with status 0 (scaffold compiles cleanly)
    - `/tmp/shadow-realm-git-backup` no longer exists (was moved back, not copied) — verify with `! test -d /tmp/shadow-realm-git-backup`
    - `.planning/` directory still exists with all original files — verify with `test -d .planning && test -f .planning/ROADMAP.md`
    - `CLAUDE.md` still exists — verify with `test -f CLAUDE.md`
  </acceptance_criteria>
  <done>Next.js 15 project scaffolded into repo root, pre-existing git history and planning artifacts preserved, local production build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Commit scaffold output and push to GitHub main</name>
  <files>
    - .git/ (commit object created)
    - All Task 1 files now staged and committed
  </files>
  <read_first>
    - Output of `git status` from Task 1 verification
    - Run `git remote -v` to confirm origin = git@github.com:cameronallen18/the-shadow-realm.git (or https variant) — RESEARCH confirms remote already configured as `cameronallen18/the-shadow-realm`
    - Run `git config user.name && git config user.email` to confirm "Cameron" / "cameronallen18@gmail.com" (per critical_context)
    - Run `git branch --show-current` to confirm current branch is `main`
  </read_first>
  <action>
Stage everything, commit, and push:

1. Stage all changes (the scaffold output + the README.md overwrite). Use `git add -A` here intentionally — there are no secrets in scaffold output, and the .gitignore generated by create-next-app already excludes node_modules, .next, .env*, etc.:
```bash
cd /home/cameron/projects/the-shadow-realm
git add -A
git status
```

Verify `git status` shows no `node_modules/`, no `.next/`, no `.env*` files staged. If any of those appear staged, STOP and inspect the .gitignore.

2. Create the commit. Use the exact message format below (no Co-Authored-By trailer for this commit since it's a scaffold, not a feature work item — keep history clean):
```bash
git commit -m "feat: scaffold Next.js 15 with Tailwind v4 and TypeScript

Generated by create-next-app@next-15-3 with flags:
--typescript --tailwind --eslint --app --no-src-dir --import-alias @/* --no-git

Preserves pre-existing .planning/, CLAUDE.md, and git history.
Closes INFRA-01."
```

3. Confirm the commit:
```bash
git log --oneline -3
git show --stat HEAD
```

4. Push to GitHub `main`:
```bash
git push origin main
```

If the push is rejected (non-fast-forward), STOP and report — do NOT force-push to main. The expected case is a clean fast-forward push since no one else writes to this repo.

5. Confirm the push succeeded by checking remote state:
```bash
git log origin/main --oneline -3
git status
```

`git status` should report "Your branch is up to date with 'origin/main'."
  </action>
  <verify>
    <automated>cd /home/cameron/projects/the-shadow-realm && git log -1 --pretty=%s | grep -q "scaffold Next.js 15" && git diff origin/main HEAD --quiet && git status | grep -q "up to date with 'origin/main'"</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit message starts with `feat: scaffold Next.js 15` — verify with `git log -1 --pretty=%s | grep -q "scaffold Next.js 15"`
    - `git status` reports "nothing to commit, working tree clean" — verify with `git status | grep -q "nothing to commit"`
    - `git status` reports "up to date with 'origin/main'" — verify with `git status | grep -q "up to date with 'origin/main'"`
    - `git diff origin/main HEAD` produces no output (local main matches remote main) — verify with `git diff origin/main HEAD --quiet`
    - `git log origin/main --oneline -1` shows the same commit hash as `git log HEAD --oneline -1` — verify with `[ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ]`
    - No `node_modules/` or `.next/` paths appear in `git ls-files` — verify with `! git ls-files | grep -E '^(node_modules|\.next)/'`
    - `.planning/ROADMAP.md`, `CLAUDE.md`, and the new Next.js files are all tracked — verify with `git ls-files | grep -q "\.planning/ROADMAP.md" && git ls-files | grep -q "^CLAUDE.md$" && git ls-files | grep -q "^app/layout.tsx$"`
  </acceptance_criteria>
  <done>Scaffold commit lives on origin/main; INFRA-01 satisfied; ready for Vercel to detect the push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User connects GitHub repo to Vercel via dashboard</name>
  <files>None (external dashboard configuration)</files>
  <read_first>
    - /home/cameron/projects/the-shadow-realm/.planning/phases/01-foundation/01-RESEARCH.md section "Vercel GitHub Integration (Dashboard Flow)"
  </read_first>
  <action>
This step requires the user to interact with the Vercel dashboard in a browser. Vercel does NOT have a non-interactive CLI flow for first-time GitHub repo import — the GitHub App permission grant is browser-only OAuth. This is one of the rare genuinely-manual steps.

Present the user with the EXACT instructions below. Do not paraphrase.

===

**MANUAL STEP — Connect Vercel to GitHub**

Open this URL in your browser: **https://vercel.com/new**

1. Sign in to Vercel (use the GitHub login if you don't already have a Vercel account).
2. Under "Import Git Repository", click "Add GitHub Account" if your GitHub account isn't already connected, and grant the Vercel GitHub App access to the `cameronallen18/the-shadow-realm` repo (you can grant access to all repos, or just this one — both work).
3. Find `cameronallen18/the-shadow-realm` in the repo list and click **Import**.
4. On the "Configure Project" screen, verify:
   - **Project Name:** `the-shadow-realm` (or whatever Vercel suggests — does not matter)
   - **Framework Preset:** `Next.js` (must auto-detect — if it shows "Other", STOP and report)
   - **Root Directory:** `./` (default — leave alone)
   - **Build Command:** `next build` (default — DO NOT change)
   - **Output Directory:** leave blank (Vercel handles natively)
   - **Install Command:** `npm install` (default)
   - **Environment Variables:** none required for Phase 1
5. Click **Deploy**.
6. Wait for the build to complete (typically 30-90 seconds for an unmodified scaffold).
7. When the build finishes, Vercel shows a "Congratulations!" screen with a preview tile and a `*.vercel.app` URL. Click the URL to open the live site in a new tab.

**Reply with:**
- The live Vercel URL (e.g. `https://the-shadow-realm-abc123.vercel.app`)
- Confirmation that the page loads without errors (you should see the default Next.js welcome page with the Next.js + Vercel logos)
- "approved" if everything looks correct, OR a description of what went wrong

===
  </action>
  <what-built>
    Task 1 + Task 2 already produced the artifacts: a Next.js 15 scaffold pushed to GitHub. This checkpoint wires that GitHub repo to Vercel so future pushes auto-deploy.
  </what-built>
  <how-to-verify>
    1. Visit https://vercel.com/new
    2. Import cameronallen18/the-shadow-realm
    3. Accept all default Configure Project settings (Framework auto-detects Next.js)
    4. Click Deploy, wait for build, click the resulting *.vercel.app URL
    5. Page loads without browser errors; Next.js welcome page is visible
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL: User must reply with live Vercel URL and 'approved' — no automated check possible for first-time OAuth GitHub App grant"</automated>
  </verify>
  <acceptance_criteria>
    - User has provided a live `https://*.vercel.app` URL
    - User has confirmed the URL loads the default Next.js welcome page in a browser
    - User has typed "approved" or equivalent confirmation
    - (For records) the URL should be saved into the SUMMARY.md as `live_url` for downstream phases
  </acceptance_criteria>
  <resume-signal>Reply with the live Vercel URL and "approved", or describe what failed (framework not detected, build failed, 404, etc.)</resume-signal>
  <done>Vercel project exists, is linked to cameronallen18/the-shadow-realm, first deploy succeeded, public URL is captured. INFRA-02 partially satisfied (link established) — Task 4 verifies the auto-deploy half.</done>
</task>

<task type="auto">
  <name>Task 4: Verify auto-deploy on push and confirm live URL responds 200</name>
  <files>
    - .planning/phases/01-foundation/.live-url.txt (scratch file holding the URL captured from Task 3 — gitignored, just for verify steps)
  </files>
  <read_first>
    - The Vercel URL provided by the user in Task 3
    - Run `git log --oneline -3` to know the current HEAD commit hash
  </read_first>
  <action>
Verify two things: (a) the URL captured in Task 3 actually responds, and (b) a fresh push to main triggers a new Vercel deploy automatically (proves the webhook is wired, not just that one deploy worked).

1. Save the Vercel URL the user provided into a scratch file (gitignored — used only for verification commands; do NOT commit):
```bash
cd /home/cameron/projects/the-shadow-realm
mkdir -p .planning/phases/01-foundation
echo "<USER_PROVIDED_VERCEL_URL>" > .planning/phases/01-foundation/.live-url.txt
echo ".planning/phases/01-foundation/.live-url.txt" >> .gitignore
```

Replace `<USER_PROVIDED_VERCEL_URL>` with the literal URL the user pasted (e.g. `https://the-shadow-realm-abc123.vercel.app`).

2. Smoke-test the live URL with curl (must return 200):
```bash
URL=$(cat /home/cameron/projects/the-shadow-realm/.planning/phases/01-foundation/.live-url.txt)
curl -sSI "$URL" | head -20
curl -sS -o /dev/null -w "%{http_code}\n" "$URL"
```

Expected: HTTP 200 status code, response headers include `server: Vercel` and `x-vercel-id: ...`.

3. Trigger a verification deploy by making a trivial change and pushing. The change must be real (Vercel does not deploy zero-diff commits) but harmless. Add a single trailing newline to README.md:
```bash
cd /home/cameron/projects/the-shadow-realm
printf "\n" >> README.md
git add README.md
git commit -m "chore: trigger Vercel auto-deploy verification

Verifies that push to main triggers a production deploy automatically.
Closes INFRA-02, INFRA-03."
git push origin main
git log --oneline -3
```

4. Tell the user to open the Vercel dashboard for this project (Deployments tab) and confirm a new deploy started within ~10 seconds of the push, then completes successfully. Capture the user's confirmation in your message before declaring this task done.

5. After the user confirms, re-curl the URL to confirm the new deploy is live:
```bash
URL=$(cat /home/cameron/projects/the-shadow-realm/.planning/phases/01-foundation/.live-url.txt)
curl -sS -o /dev/null -w "%{http_code}\n" "$URL"
```

Expected: HTTP 200 still.

6. Append the live URL to STATE.md so downstream phases can reference it:
```bash
# Will be done as part of SUMMARY.md generation in execute-plan workflow
echo "Vercel URL: $(cat /home/cameron/projects/the-shadow-realm/.planning/phases/01-foundation/.live-url.txt)"
```
  </action>
  <verify>
    <automated>cd /home/cameron/projects/the-shadow-realm && URL=$(cat .planning/phases/01-foundation/.live-url.txt) && [ -n "$URL" ] && CODE=$(curl -sS -o /dev/null -w "%{http_code}" "$URL") && [ "$CODE" = "200" ] && git log -1 --pretty=%s | grep -q "trigger Vercel auto-deploy verification" && git status | grep -q "up to date with 'origin/main'"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation/.live-url.txt` exists and contains a non-empty `https://...vercel.app` URL — verify with `test -s .planning/phases/01-foundation/.live-url.txt && grep -q "^https://" .planning/phases/01-foundation/.live-url.txt`
    - `curl -sS -o /dev/null -w "%{http_code}" $URL` returns exactly `200` — verify with `[ "$(curl -sS -o /dev/null -w '%{http_code}' $(cat .planning/phases/01-foundation/.live-url.txt))" = "200" ]`
    - `curl -sSI $URL` headers include `server: Vercel` (case-insensitive) — verify with `curl -sSI $(cat .planning/phases/01-foundation/.live-url.txt) | grep -i "^server:" | grep -qi vercel`
    - HEAD commit message contains `trigger Vercel auto-deploy verification` — verify with `git log -1 --pretty=%s | grep -q "trigger Vercel auto-deploy"`
    - User has confirmed (in chat) that the Vercel Deployments tab shows a new production deploy triggered by the trigger commit and that it succeeded
    - `.gitignore` includes the scratch URL file path so it does not get committed — verify with `grep -q ".planning/phases/01-foundation/.live-url.txt" .gitignore`
    - `git status` reports "up to date with 'origin/main'" after the trigger push — verify with `git status | grep -q "up to date with 'origin/main'"`
  </acceptance_criteria>
  <done>Live Vercel URL returns 200; auto-deploy on push verified end-to-end. INFRA-02 and INFRA-03 fully satisfied. Phase 1 success criteria all green.</done>
</task>

</tasks>

<verification>
End-of-phase checks (run after all 4 tasks complete):

1. **INFRA-01 — Next.js 15 scaffold:**
   - `cd /home/cameron/projects/the-shadow-realm && grep -q '"next": "15' package.json` → exit 0
   - `test -f app/layout.tsx && test -f app/page.tsx && test -f next.config.ts` → exit 0
   - `grep -q '@import "tailwindcss"' app/globals.css` → exit 0

2. **INFRA-02 — GitHub <-> Vercel auto-deploy:**
   - `git log origin/main --oneline -2` shows both the scaffold commit AND the trigger commit
   - User has confirmed the trigger commit produced a new Vercel production deploy in the Deployments tab

3. **INFRA-03 — Live URL accessible:**
   - `curl -sS -o /dev/null -w "%{http_code}" $(cat .planning/phases/01-foundation/.live-url.txt)` → `200`
   - URL renders the Next.js welcome page in a browser (user-confirmed in Task 3)

4. **Preservation checks:**
   - `test -d .planning && test -f CLAUDE.md && test -f .planning/ROADMAP.md` → exit 0
   - `git log --oneline | tail -5` shows the original pre-scaffold commits still in history
</verification>

<success_criteria>
Phase 1 is complete when:
- [ ] Next.js 15.x project files exist at /home/cameron/projects/the-shadow-realm root
- [ ] `npm run build` passes locally
- [ ] Scaffold commit and trigger commit both pushed to origin/main
- [ ] Vercel project linked to cameronallen18/the-shadow-realm
- [ ] At least 2 successful Vercel production deploys in Deployments tab (initial + trigger)
- [ ] Live `*.vercel.app` URL returns HTTP 200 via curl
- [ ] User has visually confirmed the live URL renders the Next.js welcome page
- [ ] Pre-existing .planning/, CLAUDE.md, and git history all preserved
- [ ] All three requirements (INFRA-01, INFRA-02, INFRA-03) verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` capturing:
- Confirmed Next.js version (e.g. 15.3.9), Tailwind version, React version (read from package.json)
- The live Vercel URL
- The two commit hashes (scaffold + trigger)
- Confirmation that .git history was preserved across the create-next-app run
- Any deviations from the plan (e.g., if create-next-app prompted unexpectedly, or Vercel framework auto-detect failed)
- Note for Phase 2: the dark theme work starts from the unmodified scaffold output — `app/layout.tsx` will need `className="dark"` added to `<html>` and `app/globals.css` will need the `@custom-variant dark` directive
</output>
