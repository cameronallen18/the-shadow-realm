---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - LAND-01
  - LAND-03

must_haves:
  truths:
    - "Page background is #0a0a0a — dark, always, no media query required"
    - "Body text renders in #ededed using Geist Sans — no Arial fallback"
    - "Page heading reads 'the shadow realm' in lowercase, large, centered"
    - "Heading has a subtle cool gradient via background-clip:text — no separate visual element"
    - "Layout is vertically and horizontally centered with min-h-screen"
    - "No scaffold content remains in page.tsx"
  artifacts:
    - path: "app/globals.css"
      provides: "Static dark CSS vars, no @media dark block, no Arial override"
      contains: "--background: #0a0a0a"
    - path: "app/layout.tsx"
      provides: "className='dark' and suppressHydrationWarning on html element"
      contains: "suppressHydrationWarning"
    - path: "app/page.tsx"
      provides: "Centered splash layout with heading, blurb placeholder"
      min_lines: 20
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "className=\"dark\""
    - from: "app/page.tsx"
      to: "app/globals.css"
      via: "CSS custom properties on body"
      pattern: "var\\(--background\\)"
---

<objective>
Wire dark mode permanently and replace the scaffold page with the real landing page layout.

Purpose: Establishes the visual foundation — dark color palette always active, Geist font rendering correctly, and the centered splash layout with the "the shadow realm" heading. The blurb content is a placeholder at this stage (finalized in plan 02-02 after checkpoint).
Output: A dark-themed landing page shell with heading, heading gradient, and placeholder blurb text. Favicon and metadata update come in plan 02-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-CONTEXT.md

<interfaces>
<!-- Current state of files being modified. Read these before touching anything. -->

From app/globals.css (current — has bugs to fix):
- Line 15-20: `@media (prefers-color-scheme: dark)` block → DELETE this entire block
- Line 22-26: `body {}` has `font-family: Arial, Helvetica, sans-serif` → DELETE that line only
- Lines 3-6: `:root` light vars → replace values with dark: `--background: #0a0a0a; --foreground: #ededed`

From app/layout.tsx (current — needs two changes):
- Line 26: `<html lang="en">` → add `className="dark"` → `<html lang="en" className="dark">`
- Lines 15-18: metadata object → update in plan 02-02, leave as-is in this plan

From app/page.tsx (current — fully replaced):
- Entire file → replace. Do not keep any imports or JSX from scaffold.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix globals.css — dark-always palette, remove Arial, remove media query</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current state — understand what exists before modifying)
    - .planning/phases/02-landing-page/02-CONTEXT.md (D-01 through D-07)
  </read_first>
  <action>
Rewrite app/globals.css to:

1. Keep `@import "tailwindcss"` as the first line — do not remove it.

2. Replace the `:root` block with static dark-always values (no light/dark split):
   ```css
   :root {
     --background: #0a0a0a;
     --foreground: #ededed;
   }
   ```

3. Keep the `@theme inline` block unchanged — it maps CSS vars to Tailwind tokens and must stay:
   ```css
   @theme inline {
     --color-background: var(--background);
     --color-foreground: var(--foreground);
     --font-sans: var(--font-geist-sans);
     --font-mono: var(--font-geist-mono);
   }
   ```

4. DELETE the entire `@media (prefers-color-scheme: dark)` block (lines 15-20). Per D-06: dark is always active via className="dark", no media query needed.

5. Rewrite the `body {}` block — remove `font-family: Arial, Helvetica, sans-serif` entirely (per D-07 WR-01). Keep background and color:
   ```css
   body {
     background: var(--background);
     color: var(--foreground);
   }
   ```
   Font is handled by Tailwind's `--font-sans` token via `@theme inline` above.

Final file should be ~18 lines. No media queries. No Arial.
  </action>
  <verify>
    <automated>grep -n "Arial" /home/cameron/projects/the-shadow-realm/app/globals.css && echo "FAIL: Arial still present" || echo "PASS: Arial removed"; grep -n "prefers-color-scheme" /home/cameron/projects/the-shadow-realm/app/globals.css && echo "FAIL: media query still present" || echo "PASS: media query removed"; grep -c "#0a0a0a" /home/cameron/projects/the-shadow-realm/app/globals.css | grep -q "1" && echo "PASS: dark background set" || echo "FAIL: #0a0a0a not found"</automated>
  </verify>
  <done>globals.css has static #0a0a0a background, #ededed foreground, no Arial, no @media dark block, and @theme inline intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire dark class on html and build the landing page</name>
  <files>app/layout.tsx, app/page.tsx</files>
  <read_first>
    - app/layout.tsx (add className="dark" to html element — per D-05)
    - app/page.tsx (full replacement — per D-26)
    - .planning/phases/02-landing-page/02-CONTEXT.md (D-05, D-09 through D-16)
  </read_first>
  <action>
**app/layout.tsx — single change:**

Add `className="dark"` to the `<html>` element (per D-05). Keep all other code unchanged — do not update metadata yet (that happens in plan 02-02 after blurb approval).

Change line 26 from:
```tsx
<html lang="en">
```
to:
```tsx
<html lang="en" className="dark" suppressHydrationWarning>
```

**app/page.tsx — full replacement:**

Replace the entire file with a centered splash layout. No `next/image` import needed. No scaffold content.

```tsx
export default function Home() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center px-6">
      <div className="flex flex-col items-center gap-6 text-center max-w-lg">
        <h1 className="heading-gradient lowercase tracking-tight leading-none">
          the shadow realm
        </h1>
        <p className="text-[#ededed]/60 text-sm tracking-wide">
          {/* Blurb placeholder — finalized in plan 02-02 after checkpoint */}
          a place for things to land.
        </p>
      </div>
    </main>
  );
}
```

**Heading gradient — add to globals.css as a component class** (append after the body block):

```css
.heading-gradient {
  font-size: clamp(2.5rem, 6vw, 5rem);
  font-family: var(--font-geist-sans);
  letter-spacing: -0.03em;
  background: linear-gradient(
    135deg,
    #ededed 0%,
    #c8cdd4 50%,
    #9ba3ad 100%
  );
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
  -webkit-text-fill-color: transparent;
}
```

Gradient rationale (per D-14, D-15, D-16, Claude's discretion):
- `#ededed` → `#c8cdd4` → `#9ba3ad`: off-white to cool silver-gray — monochrome, cool-toned, static
- 135deg angle gives diagonal depth without drama
- `background-clip: text` with `color: transparent` is the correct cross-browser approach (webkit prefix required)

The blurb text in page.tsx is a placeholder. It will be replaced in plan 02-02 after user approval. The comment makes this explicit.
  </action>
  <verify>
    <automated>grep -n 'className="dark"' /home/cameron/projects/the-shadow-realm/app/layout.tsx && echo "PASS: dark class on html" || echo "FAIL: dark class missing"; grep -n "the shadow realm" /home/cameron/projects/the-shadow-realm/app/page.tsx && echo "PASS: heading present" || echo "FAIL: heading missing"; grep -n "heading-gradient" /home/cameron/projects/the-shadow-realm/app/globals.css && echo "PASS: gradient class present" || echo "FAIL: gradient class missing"; grep -n "background-clip" /home/cameron/projects/the-shadow-realm/app/globals.css && echo "PASS: background-clip set" || echo "FAIL: background-clip missing"</automated>
  </verify>
  <done>layout.tsx has className="dark" on html, page.tsx is a centered splash with heading + placeholder blurb, globals.css has .heading-gradient with clamp sizing and cool gradient via background-clip:text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML → browser | All content is server-rendered static HTML. No user input, no API routes, no dynamic data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | app/page.tsx external links (if any added) | accept | No external links in this plan. If added in future, use rel="noopener noreferrer". |
| T-02-02 | Tampering | CSS custom properties | accept | No user-controlled CSS injection. All values are static build-time constants. |
| T-02-03 | Information Disclosure | Source maps | accept | Next.js default build omits source maps in production. No secrets in source. |

Note: This is a static landing page with no auth, no user input, no API routes, and no secrets in source. STRIDE surface is minimal. The primary future concern (external links) is addressed with rel="noopener noreferrer" as a standing convention.
</threat_model>

<verification>
Build passes without errors:
```bash
cd /home/cameron/projects/the-shadow-realm && npm run build 2>&1 | tail -20
```

Dark class is set:
```bash
grep 'className="dark"' /home/cameron/projects/the-shadow-realm/app/layout.tsx
```

No warm colors or Arial in CSS:
```bash
grep -E "Arial|warm|orange|red|yellow|prefers-color-scheme" /home/cameron/projects/the-shadow-realm/app/globals.css
```

Page content is real (not scaffold):
```bash
grep -L "Create Next App\|vercel.com\|next.svg" /home/cameron/projects/the-shadow-realm/app/page.tsx
```
</verification>

<success_criteria>
- `npm run build` completes without TypeScript or compilation errors
- `app/globals.css` contains `--background: #0a0a0a`, no Arial, no @media dark block
- `app/layout.tsx` has `className="dark"` on the html element
- `app/page.tsx` renders "the shadow realm" heading with `.heading-gradient` class
- `.heading-gradient` in globals.css uses `clamp(2.5rem, 6vw, 5rem)`, cool gradient, `background-clip: text`
- No scaffold content remains (no next.svg, vercel.svg references, no "Create Next App" text)
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md` using the summary template.
</output>
