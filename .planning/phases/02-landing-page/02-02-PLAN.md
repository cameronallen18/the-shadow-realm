---
phase: 02-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/page.tsx
  - app/layout.tsx
  - app/icon.svg
  - public/next.svg
  - public/vercel.svg
  - public/file.svg
  - public/globe.svg
  - public/window.svg
autonomous: false
requirements:
  - DESIGN-03
  - LAND-02

must_haves:
  truths:
    - "User-approved blurb text is present in the page — not a placeholder comment"
    - "Favicon renders as an 'S' letter mark in the browser tab"
    - "Browser tab title shows 'the shadow realm' (lowercase)"
    - "Scaffold SVGs in public/ are deleted"
    - "Metadata description matches the approved blurb tone"
  artifacts:
    - path: "app/icon.svg"
      provides: "SVG favicon — 'S' letter mark, white on #0a0a0a"
      contains: "<text"
    - path: "app/page.tsx"
      provides: "Final blurb text (not placeholder)"
    - path: "app/layout.tsx"
      provides: "Updated metadata: title 'the shadow realm', description matching blurb"
      contains: "the shadow realm"
  key_links:
    - from: "app/icon.svg"
      to: "browser tab"
      via: "Next.js App Router icon file convention"
      pattern: "icon\\.svg"
    - from: "app/layout.tsx"
      to: "metadata.title"
      via: "Next.js Metadata API"
      pattern: "title.*the shadow realm"
---

<objective>
Finalize the blurb copy (with user approval), install the favicon, update metadata, and remove scaffold SVGs.

Purpose: Completes the landing page. The checkpoint ensures the blurb — which defines the site's personality — ships with the owner's approval rather than Claude's best guess. Favicon and metadata finalization follow the approved blurb so description can match tone.
Output: Production-ready landing page with approved blurb, correct favicon, correct metadata, and no scaffold artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Blurb copy — present drafts, user approves</name>
  <files>app/page.tsx</files>
  <read_first>
    - .planning/phases/02-landing-page/02-CONTEXT.md (D-17, D-18, D-19 for tone and constraints)
    - app/page.tsx (see current placeholder — will be replaced with approved text)
  </read_first>
  <action>Present blurb options to user and wait for approval. No file changes in this task — changes happen in Task 2 after approval.</action>
  <what-built>
Three blurb options for the landing page, written per D-18 (understated, personal, slightly dry — communicates personal placeholder / project dumping ground). All are 1-2 short sentences max (D-19).

**Option A:**
> a place for things to land.

**Option B:**
> personal projects, work in progress, nothing to see yet.

**Option C:**
> somewhere to put things. more eventually.

**Option D (two-sentence):**
> a personal placeholder. projects accumulate here over time.

Tone notes: all options avoid boasting, avoid marketing language, avoid warmth. "the shadow realm" already sets the register — the blurb should feel like a natural extension of that same voice.
  </what-built>
  <how-to-verify>
Read the options above and pick the one that fits, OR provide your own wording. The only constraints are:
- 1-2 short sentences maximum (D-19)
- Understated, slightly dry tone — no marketing language (D-18)
- Communicates: personal placeholder / project dumping ground (LAND-02)

After you approve, the executor will replace the placeholder in app/page.tsx and use the approved text as the basis for the metadata description in app/layout.tsx.
  </how-to-verify>
  <resume-signal>Type "approved: [option letter]" or "approved: [your custom wording]"</resume-signal>
  <verify>
    <automated>echo "Checkpoint — no automated verification. Resume after user provides approved blurb string."</automated>
  </verify>
  <acceptance_criteria>
    User has provided an approved blurb string. The string is 1-2 sentences, understated in tone.
  </acceptance_criteria>
  <done>User has approved blurb copy — a specific string is confirmed for use in page.tsx and as the basis for metadata description.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert approved blurb, create favicon, update metadata, delete scaffold SVGs</name>
  <files>app/page.tsx, app/layout.tsx, app/icon.svg, public/next.svg, public/vercel.svg, public/file.svg, public/globe.svg, public/window.svg</files>
  <read_first>
    - app/page.tsx (replace placeholder blurb with approved text from Task 1)
    - app/layout.tsx (update metadata title and description — per D-23, D-24)
    - .planning/phases/02-landing-page/02-CONTEXT.md (D-20, D-21, D-22 for favicon spec)
  </read_first>
  <action>
**1. app/page.tsx — replace placeholder blurb:**

Find the `<p>` blurb element (currently contains the placeholder comment and "a place for things to land."). Replace the text content with the user-approved blurb from Task 1. Remove the placeholder comment. Keep all Tailwind classes on the `<p>` tag unchanged.

Example (substitute approved text):
```tsx
<p className="text-[#ededed]/60 text-sm tracking-wide">
  [approved blurb text here]
</p>
```

**2. app/layout.tsx — update metadata:**

Replace the metadata export with:
```tsx
export const metadata: Metadata = {
  title: "the shadow realm",
  description: "[short description derived from approved blurb — same tone, can be identical or a slight variation]",
};
```

Per D-23: title is "the shadow realm" (lowercase — this is intentional).
Per D-24: description should match blurb tone. If blurb is "a place for things to land." the description can be "a place for things to land — personal projects and work in progress." or simply the blurb text verbatim. Keep it short and dry.

**3. app/icon.svg — create SVG favicon:**

Create this file at `app/icon.svg` (Next.js App Router serves this as the favicon automatically via the icon file convention — no additional config needed).

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <rect width="32" height="32" fill="#0a0a0a"/>
  <text
    x="16"
    y="23"
    font-family="system-ui, -apple-system, sans-serif"
    font-size="20"
    font-weight="500"
    fill="#ededed"
    text-anchor="middle"
    dominant-baseline="auto"
  >S</text>
</svg>
```

Notes (per D-20, D-21, D-22):
- Background `#0a0a0a` matches site background; "S" in `#ededed` — white letter mark
- `system-ui` font stack used because SVG text rendering doesn't have access to next/font loaded fonts; system-ui is the closest sans-serif match to Geist at favicon size
- `viewBox="0 0 32 32"` — standard favicon dimensions
- Next.js App Router detects `app/icon.svg` and serves it as the favicon automatically

**4. Delete scaffold SVGs from public/ (per D-27):**

These are no longer referenced by any page after plan 02-01 replaced page.tsx. Use the Bash tool:
```bash
rm /home/cameron/projects/the-shadow-realm/public/next.svg \
   /home/cameron/projects/the-shadow-realm/public/vercel.svg \
   /home/cameron/projects/the-shadow-realm/public/file.svg \
   /home/cameron/projects/the-shadow-realm/public/globe.svg \
   /home/cameron/projects/the-shadow-realm/public/window.svg
```

Do NOT delete `public/` itself — Phase 3 uses it for math flash card assets (D-28).
  </action>
  <verify>
    <automated>
# Favicon exists
test -f /home/cameron/projects/the-shadow-realm/app/icon.svg && echo "PASS: icon.svg exists" || echo "FAIL: icon.svg missing"

# Scaffold SVGs gone
for f in next.svg vercel.svg file.svg globe.svg window.svg; do
  test -f "/home/cameron/projects/the-shadow-realm/public/$f" && echo "FAIL: $f still exists" || echo "PASS: $f deleted"
done

# public/ directory still exists
test -d /home/cameron/projects/the-shadow-realm/public && echo "PASS: public/ retained" || echo "FAIL: public/ deleted"

# Metadata updated
grep -n "the shadow realm" /home/cameron/projects/the-shadow-realm/app/layout.tsx && echo "PASS: title updated" || echo "FAIL: title not updated"

# Placeholder comment gone from page.tsx
grep -n "Blurb placeholder" /home/cameron/projects/the-shadow-realm/app/page.tsx && echo "FAIL: placeholder still present" || echo "PASS: placeholder removed"

# Build passes
cd /home/cameron/projects/the-shadow-realm && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    test -f /home/cameron/projects/the-shadow-realm/app/icon.svg
    grep -q "the shadow realm" /home/cameron/projects/the-shadow-realm/app/layout.tsx
    ! test -f /home/cameron/projects/the-shadow-realm/public/next.svg
    ! grep -q "Blurb placeholder" /home/cameron/projects/the-shadow-realm/app/page.tsx
    cd /home/cameron/projects/the-shadow-realm && npm run build 2>&1 | grep -q "Route (app)"
  </acceptance_criteria>
  <done>Approved blurb is in page.tsx, metadata reflects "the shadow realm" title, app/icon.svg exists as SVG favicon, all five scaffold SVGs are deleted, and npm run build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — dark page, favicon, heading, blurb</name>
  <files>app/page.tsx, app/icon.svg</files>
  <read_first>
    - app/page.tsx (confirm blurb text is as approved)
    - app/icon.svg (confirm SVG is well-formed)
  </read_first>
  <action>Run `npm run dev`, then pause for user to visually verify the complete landing page in a browser before proceeding.</action>
  <what-built>
The complete landing page:
- Dark background (#0a0a0a), heading "the shadow realm" with cool gradient, approved blurb below
- Favicon "S" letter mark in browser tab
- Browser tab title "the shadow realm"
- No scaffold content anywhere
  </what-built>
  <how-to-verify>
1. Run the dev server: `cd /home/cameron/projects/the-shadow-realm && npm run dev`
2. Open http://localhost:3000 in a browser
3. Confirm:
   - Background is near-black (not white, not gray)
   - "the shadow realm" heading is large, centered, lowercase, with a subtle gradient quality
   - Approved blurb text appears below the heading in smaller, dimmer text
   - No Next.js scaffold content is visible (no logos, no links, no "Get started by editing")
4. Check the browser tab:
   - Tab title reads "the shadow realm"
   - Favicon shows the "S" letter mark (may take a hard refresh: Ctrl+Shift+R)
5. Resize the window to confirm heading scales gracefully (clamp behavior)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks right, or describe any issues to fix</resume-signal>
  <verify>
    <automated>echo "Checkpoint — visual verification by user. Resume after user confirms appearance."</automated>
  </verify>
  <acceptance_criteria>
    User has confirmed: dark background visible, heading present and styled, blurb present with approved text, favicon renders in tab, tab title is correct.
  </acceptance_criteria>
  <done>User has visually confirmed the landing page matches the design intent — dark, minimal, heading with gradient, approved blurb, correct favicon and tab title.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML → browser | All content is server-rendered static HTML. No user input, no API routes, no dynamic data. |
| SVG favicon → browser | SVG is a static file with no scripts or external references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | app/icon.svg | accept | SVG is static, no scripts, no external src. Browser renders as image only. |
| T-02-05 | Tampering | metadata description | accept | Metadata is static string, server-rendered. No user input reaches this field. |
| T-02-06 | Information Disclosure | public/ directory | accept | Only the math flash card game assets will live here in Phase 3. No secrets. Directory listing is disabled by Next.js by default. |

Note: Static landing page. No auth, no user input, no API routes, no secrets in source. Threat surface remains minimal across both plans in this phase.
</threat_model>

<verification>
All success criteria verified together after Task 2 and Task 3 complete:

```bash
# Build passes cleanly
cd /home/cameron/projects/the-shadow-realm && npm run build 2>&1 | grep -E "error|Route \(app\)"

# Favicon present
test -f /home/cameron/projects/the-shadow-realm/app/icon.svg && echo "favicon: ok"

# Title updated
grep "the shadow realm" /home/cameron/projects/the-shadow-realm/app/layout.tsx

# Scaffold SVGs gone
ls /home/cameron/projects/the-shadow-realm/public/

# No placeholder comments in page.tsx
grep -c "placeholder" /home/cameron/projects/the-shadow-realm/app/page.tsx
```
</verification>

<success_criteria>
- User has approved the blurb text in Task 1 checkpoint
- Approved blurb text is live in app/page.tsx — no placeholder comment
- app/icon.svg exists and is valid SVG with "S" letter mark on #0a0a0a background
- app/layout.tsx metadata title is "the shadow realm" (lowercase)
- app/layout.tsx metadata description is short, dry, matches blurb tone
- public/ contains no scaffold SVGs (next.svg, vercel.svg, file.svg, globe.svg, window.svg)
- public/ directory itself still exists (Phase 3 dependency)
- npm run build passes without errors
- User has visually confirmed the complete page in Task 3 checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-02-SUMMARY.md` using the summary template.
</output>
