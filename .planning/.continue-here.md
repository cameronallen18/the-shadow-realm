---
context: default
phase: null
status: paused
last_updated: 2026-05-01T14:48:48.853Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Idle animation is wrong — the idle/gameover screens now have a broken RAF loop that animates Samus running in place. User wants STATIC pose on idle. The Playwright test for "running animation" passes but tests this wrong behavior. Do NOT trust that test as a correctness signal.

**Do not proceed until this box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Test validates wrong behavior | The "running animation advances frames" Playwright test checks the idle screen RAF loop — which is the regression, not the feature. Test passes = green but the behavior is incorrect. | blocking | Rewrite the test to assert idle=static (frame 0 only, no pixel change over time) and in-game-ground=running before closing this issue. |
| Idle RAF loop | Effect A in SamusRunGame.tsx was changed from single-render+ResizeObserver to a continuous RAF loop. This is wrong — it animates Samus on the title screen when she should stand still. | blocking | Revert Effect A to the original pattern: call render() once, then ResizeObserver for repaints. |

<current_state>
Ad-hoc bug fix session just completed. 5 bugs were fixed (background, hitbox, sprite borders, removed block figure) and committed as v0.1.16. Playwright tests added as v0.1.17. One regression was introduced and not caught: idle screen animation was added (Effect A RAF loop) but user wants static idle pose. The in-game ground running animation (Effect B) was already correct — Samus uses runRight frames when !isAirborne during gameplay.
</current_state>

<completed_work>

- ✅ Background: replaced norfair tileset atlas (wrong asset) with procedural cave draw
- ✅ Hitbox: fixed center-anchor bug (samusY = feet, not center); samusHeight 60→44
- ✅ Sprite left border: sx shifted +1 to skip black cell border pixel at x=17
- ✅ Sprite bottom border: sh reduced 70→56, dy anchored to foot row 42 (pixel-inspected)
- ✅ Block figure: removed drawSamusIdle + drawSamusJump
- ✅ Playwright tests: 7 tests, all passing (but one tests wrong behavior — see blocker)
- ✅ Pushed to main, Vercel deploying v0.1.17
</completed_work>

<remaining_work>

**Task 7 (open regression): Fix idle/game animation behavior**

The correct behavior:
- **Idle / game-over screens**: Static standing pose, frame 0 of runRight, no animation loop
- **During gameplay, on ground**: Running animation (runRight, 4 frames, 10fps) — this already works in Effect B
- **During gameplay, in air**: Screw attack spin (screwAttackL, 9 frames, 10fps) — this already works in Effect B

What to fix in `components/samus-run/SamusRunGame.tsx`:
1. **Effect A** (lines ~130–155): Revert from RAF loop back to `render()` once + ResizeObserver. Pass `animState = undefined` (or `{ frame: 0, ... }`) so idle shows frame 0 only.
2. **Playwright test** (`tests/samus-run.spec.ts`, last test): Rewrite "running animation" test to assert idle=static (two screenshots 400ms apart should have minimal diff in Samus region). Add a separate in-game test: click to start, wait a moment for landing, check Samus region changes over time.
</remaining_work>

<decisions_made>

- norfair_upper.png and norfair_lower.png are Metroid tileset sprite atlases — not usable as BG tiles. Procedural cave is the permanent solution unless new proper BG tiles are sourced.
- COLLISION.samusHeight is now 44 (actual pixel-inspected sprite height head-to-feet). samusWidth stays at 40 (intentionally wider than sprite for fair hitbox).
- Sprite source rect: sx = frameIndex*96 + 18 (contentOffset+1), sw = 75 (contentSize-6), sy = section.sy + 17, sh = 56 (contentSize-25). dy = y - 42 (FOOT_ROW * scale). These values are pixel-inspected and correct.
- The `runRight` section in constants is labeled "facing right" but the sprite actually faces LEFT. Not a bug per user — left for now.
</decisions_made>

<blockers>

- **Regression (idle animation)**: Effect A RAF loop ships wrong behavior. Fix is ~10 lines. Medium priority — game is playable, this is a cosmetic UX issue.
- **Human verification**: Vercel deploy of v0.1.17 in progress. User should verify background, hitbox feel, and sprite edges on device once deployed.
</blockers>

## Required Reading (in order)
1. `components/samus-run/SamusRunGame.tsx` — Effect A (idle render) and Effect B (game loop) to understand the animation split
2. `components/samus-run/canvas/drawSamus.ts` — current sprite source rect constants (pixel-inspected values)
3. `tests/samus-run.spec.ts` — the failing test that currently passes for the wrong reason

## Infrastructure State
- Dev server: may be running on localhost:3000 (started during this session with `npm run dev &`)
- Vercel: auto-deploying v0.1.17 from main branch push
- Playwright: installed in project, config at `playwright.config.ts`, uses chromium at `~/.cache/ms-playwright/chromium-1217/`

<context>
This was an unplanned bug-fix session triggered by user reporting 5 visual issues. All 5 were fixed via pixel-level inspection of the sprite sheet. The animation behavior (idle vs. gameplay) was slightly misunderstood — the fix added animation where the user wanted static, and the test was written to match the (wrong) implementation rather than the intended behavior. The sprite source rect math is now fully pixel-validated and unlikely to need revisiting.
</context>

<next_action>
Start with: Fix Effect A in SamusRunGame.tsx — replace RAF loop with single render + ResizeObserver (revert to original pattern). Then rewrite the Playwright "running animation" test to assert idle=static. Then take a Playwright screenshot during gameplay to verify in-game ground running is visible.
</next_action>
